function LoginPage() {
  return (
    <section className="flex min-h-dvh flex-col justify-center">
      <h1 className="text-2xl font-bold">로그인</h1>
      <p className="mt-2 text-sm text-slate-50">
        일정 만들기는 로그인 후 사용할 수 있습니다.
      </p>
    </section>
  );
}

export default LoginPage;
